[package]
name = "test_rs"
version = "0.1.0"
edition = "2024"

[dependencies]
pathmap = { path = "../../PathMap" }
fast-slice-utils = { version = "0.1.2", features=[] }
synth_macro = { path = "synth_macro" }

[dev-dependencies]
synth_jit = { path = "synth_jit" }
# arena_compact only for tests (act_big_hex.rs)
pathmap = { path = "../../PathMap", features = ["arena_compact"] }